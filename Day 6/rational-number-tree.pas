program rational-number;

var
    p, q, t, n, l, id : integer;
    b, t : string;
    arr : a
    f, fo : text;
    
begin
    assign(f, 'input.inp'); reset(f);
    assign(fo, 'output.out'); rewrite(fo);
        readln(f, t);
        for l := 1 to t do begin
            read(f, id);
            if id = 1 then begin
                read(f, n);
                while n <> 0 do begin
                    str(n div 2, t);
                    s := t + s;
                    n := n mod 2;
                end;
                for l1 := 2 to length(s) do begin
                    if s[l1] ==
                end;
            end else begin

            end;
            readln(f)
        end;
    close(f);
    close(fo);
end.
